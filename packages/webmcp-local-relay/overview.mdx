---
title: "@mcp-b/webmcp-local-relay overview"
sidebarTitle: "Overview"
description: "Bridge browser-hosted WebMCP tools to desktop MCP clients like Claude Desktop, Cursor, and Claude Code through a local WebSocket relay."
keywords: ["@mcp-b/webmcp-local-relay", "local relay", "desktop agent", "stdio", "websocket", "claude desktop", "cursor"]
icon: "satellite-dish"
---

`@mcp-b/webmcp-local-relay` runs a local server that forwards WebMCP tools from browser tabs to desktop MCP clients. Desktop agents like Claude Desktop, Cursor, Windsurf, and Claude Code connect over stdio, while browser pages connect over a localhost WebSocket.

## How it works

```text
Browser Tab                          Local Machine
┌──────────────────────┐             ┌──────────────────────┐
│                      │  WebSocket  │                      │
│   Website with       ├────────────►  webmcp-local-relay   │
│   WebMCP tools       │  localhost  │   (MCP server)       │
│                      │             │                      │
└──────────────────────┘             └──────────┬───────────┘
                                                │
                                          stdio │ JSON-RPC
                                                │
                                     ┌──────────▼───────────┐
                                     │                      │
                                     │   Claude Desktop /   │
                                     │   Cursor / Claude    │
                                     │   Code / Windsurf    │
                                     │                      │
                                     └──────────────────────┘
```

The relay has three parts:

1. **RelayBridgeServer** accepts WebSocket connections from browser tabs. Each tab sends a `hello` message with metadata (tab ID, origin, URL, title) and its list of registered tools.
2. **LocalRelayMcpServer** exposes those tools to MCP clients over stdio. When a tool set changes (tabs open, reload, or close), the relay sends a `toolListChanged` notification so the client sees the update.
3. **Browser embed script** injects a hidden iframe into the host page. The iframe opens the WebSocket connection and forwards tool calls between the relay and `navigator.modelContext`.

## Quick start

Add this block to your MCP client configuration (Claude Desktop, Cursor, or similar):

```json "MCP client config"
{
  "mcpServers": {
    "webmcp-local-relay": {
      "command": "npx",
      "args": ["-y", "@mcp-b/webmcp-local-relay@latest"]
    }
  }
}
```

Then add the embed script to any page that registers WebMCP tools:

```html
<script src="https://cdn.jsdelivr.net/npm/@mcp-b/webmcp-local-relay@latest/dist/browser/embed.js"></script>
```

The relay starts on `ws://127.0.0.1:9333` by default. Tools registered on the page through [`@mcp-b/global`](/packages/global/overview) or the [WebMCP polyfill](/packages/webmcp-polyfill/reference) are forwarded to the desktop client automatically.

## When to use this package

- You want a desktop AI agent to call tools exposed by a website.
- You want a browser-to-localhost bridge without DevTools Protocol automation.
- You need multiple MCP clients to share the same browser tool sources.

## When not to use this package

- You need browser automation, console access, or DOM inspection. Use [`@mcp-b/chrome-devtools-mcp`](/packages/chrome-devtools-mcp/overview) instead.
- You only need in-browser tool discovery through a runtime or extension.

## Key behaviors

**Dynamic tool registration.** Tools registered on webpages appear as first-class MCP tools. When multiple tabs register a tool with the same name, the relay disambiguates with a short tab-ID suffix (e.g., `search_ed93`, `search_a1b2`). Tools appear and disappear as tabs connect and disconnect.

**Client mode.** When a second relay instance starts and the default port is in use, it falls back to client mode. In client mode, the relay connects as a WebSocket client to the existing server and proxies tool operations through it. If the server stops, the client promotes itself back to server mode. This lets multiple MCP clients share the same browser connections.

**Static management tools.** The relay always exposes four built-in tools: `webmcp_list_sources`, `webmcp_list_tools`, `webmcp_call_tool`, and `webmcp_open_page`. These let the desktop agent discover connected tabs, list available tools, invoke tools by name, and open or refresh browser pages. See the [reference page](/packages/webmcp-local-relay/reference) for details on each tool.

**Security.** The relay binds to `127.0.0.1` (loopback only) by default. Use `--widget-origin` to restrict which host page origins can register tools. For more on the security model, see [Security and human-in-the-loop](/explanation/design/security-and-human-in-the-loop).

## Where it sits in the package graph

This package is adjacent to the runtime stack rather than inside it. Pages expose WebMCP tools through [`@mcp-b/global`](/packages/global/overview) or the polyfill, and the relay forwards those tools to desktop clients. For a conceptual discussion of how this fits alongside other transport options, see [Transports and bridges](/explanation/architecture/transports-and-bridges).

## Related pages

<CardGroup cols={2}>
  <Card title="Reference" icon="book-open" href="/packages/webmcp-local-relay/reference">
    CLI options, exported API, architecture layers, and troubleshooting.
  </Card>
  <Card title="Connect desktop agents" icon="tower-broadcast" href="/how-to/connect-desktop-agents-with-local-relay">
    Step-by-step setup for desktop MCP clients.
  </Card>
  <Card title="Desktop agent relay tutorial" icon="graduation-cap" href="/tutorials/desktop-agent-relay">
    Guided walkthrough from install to first tool call.
  </Card>
  <Card title="Transports and bridges" icon="bridge" href="/explanation/architecture/transports-and-bridges">
    How the relay compares to other bridge options.
  </Card>
</CardGroup>
