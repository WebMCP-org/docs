{/*
# React WebMCP Imports

Standard import statements for React WebMCP setup.

## When to use:
- Starting a new React component with WebMCP
- Setting up tool registration in React

## Example usage in docs:
```mdx
import CodeBlock from '/snippets/imports/react-imports.mdx';

<CodeBlock />
```
*/}

```tsx
import '@mcp-b/global';
import { useWebMCP } from '@mcp-b/react-webmcp';
import { z } from 'zod';
```
